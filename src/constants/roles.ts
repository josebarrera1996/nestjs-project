// Roles
export enum ROLES {
    BASIC = 'BASIC',
    CREATOR = 'CREATOR',
    ADMIN = 'ADMIN',
}

// Niveles de acceso
export enum ACCESS_LEVEL {
    DEVELOPER = 30,
    MANTEINER = 40,
    OWNER = 50,
}